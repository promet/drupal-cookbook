<%= File.dirname(node['drupal']['dir']) %>/*.log {
        weekly
        missingok
        rotate 52
        compress
        delaycompress
        notifempty
        sharedscripts
        postrotate
                /usr/local/bin/supervisorctl restart lsyncd > /dev/null
        endscript
}
